<!DOCTYPE html>
<html>
@await Html.PartialAsync("/Views/Admin/PartialHead.cshtml")
<body>
    <!-- Sidenav -->
     @await Html.PartialAsync("/Views/Admin/PartialSidenav.cshtml")
    <!-- Main content -->
    <div class="main-content" id="panel">
        <!-- Topnav -->
        @await Html.PartialAsync("/Views/Admin/PartialNavbar.cshtml")
        <!-- Header -->
        <!-- Header -->
      @await Html.PartialAsync("/Views/Admin/PartialHeader.cshtml")
        <!-- Page content -->
        <div class="container-fluid mt--6">
            <div class="row">
                <div class="col">
                    @RenderBody()
                </div>
            </div>
            <br />
            <br />
            <!-- Footer -->
           @await Html.PartialAsync("/Views/Admin/PartialFooter.cshtml")
        </div>
    </div>
    <!-- Argon Scripts -->
    <!-- Core -->
    @await Html.PartialAsync("/Views/Admin/PartialScripts.cshtml")
</body>
</html>